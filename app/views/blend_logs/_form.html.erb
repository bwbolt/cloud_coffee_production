<%= form_with(model: blend_log, local: true) do |form| %>
  <% if blend_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blend_log.errors.count, "error") %> prohibited this blend_log from being saved:</h2>

      <ul>
      <% blend_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :blend_id %>
    <%= form.text_field :blend_id %>
  </div>

  <div class="field">
    <%= form.label :log_id %>
    <%= form.text_field :log_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
